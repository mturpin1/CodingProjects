let the_north = ['starks', 'mormonts', 'freys']
let the_south = ['lannisters', 'tyrells', 'baratheons']

for (let count = 0 ; count < the_north.length ; count ++) {
    console.log(`The ${the_north[count]} live in the North.`)
}

for (let count1 = 0 ; count1 < the_south.length ; count1 ++) {
    console.log(`The ${the_south[count1]} live in the South.`)
}